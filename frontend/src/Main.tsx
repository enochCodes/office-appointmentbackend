import React from 'react';

const Main: React.FC = () => {
  return (
    <main className="p-5 text-center">
      <p>This is a clean and simple frontend built with React, TypeScript, and Tailwind CSS.</p>
    </main>
  );
}

export default Main;
